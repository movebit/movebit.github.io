<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Bootstrap of Sui local testnet</title>
      <link href="//post/02-Bootstrap%20of%20Sui%20local%20testnet%207860ec878e8f42c98a7c754b3afd2d30.html"/>
      <url>//post/02-Bootstrap%20of%20Sui%20local%20testnet%207860ec878e8f42c98a7c754b3afd2d30.html</url>
      
        <content type="html"><![CDATA[<p><img src="/../img/111.png" alt="Untitled"><br>Since the code of sui is updated very quickly and we do not need to connect to devnet, so we select the main branch to analyze.</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">branch maincommit db5a090d56c8f1f065474f26a12e97dc0ee33e12</code></pre><p><img src="/../img/300.png" alt="Untitled"></p><h1 id="Sui-package"><a href="#Sui-package" class="headerlink" title="Sui package"></a>Sui package</h1><p>The Sui package is in <code>sui/crates/sui</code> folder. It will be compiled into the sui command line tool. The files structure of this package is as follows:</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">➜  sui git:(main) tree.├── Cargo.toml├── build.rs├── genesis.md├── offline_signing.md├── src│   ├── client_commands.rs│   ├── config│   │   └── mod.rs│   ├── console.rs│   ├── genesis_ceremony.rs│   ├── keytool.rs│   ├── lib.rs│   ├── main.rs│   ├── shell.rs│   ├── sui_commands.rs│   ├── sui_move│   │   ├── build.rs│   │   ├── coverage.rs│   │   ├── disassemble.rs│   │   ├── mod.rs│   │   ├── new.rs│   │   ├── prove.rs│   │   ├── sui-natives.bpl│   │   └── unit_test.rs│   └── unit_tests│       ├── cli_tests.rs│       ├── data│       │   ├── dummy_modules_publish│       │   │   ├── Move.toml│       │   │   └── sources│       │   │       └── trusted_coin.move│       │   └── move_call_args_linter│       │       ├── Move.toml│       │       └── sources│       │           └── object_basics.move│       ├── keytool_tests.rs│       └── shell_tests.rs└── tests    ├── full_node_tests.rs    ├── move_test_code    │   ├── Move.toml    │   └── sources    │       └── shared_objects_version.move    ├── quorum_driver_tests.rs    ├── readme.rs    ├── reconfiguration_tests.rs    ├── shared_objects_tests.rs    ├── shared_objects_version_tests.rs    ├── simulator_tests.rs    └── transaction_orchestrator_tests.rs12 directories, 38 files</code></pre><h1 id="sui-genesis"><a href="#sui-genesis" class="headerlink" title="sui genesis"></a>sui genesis</h1><p>To create the configuration files and objects for a local Sui network, run the <code>genesis</code> command. </p><p>Genesis creates the network configuration files in the ~&#x2F;.sui&#x2F;sui_config folder. This includes a YAML file for fullnode, network, client, and each validator. It also creates a sui.keystore that stores client key pairs.</p><p>Let’s first look at how this command works.</p><blockquote><p>Entrance：<code>sui/crates/sui/src/main.rs —&gt; fn main()</code></p></blockquote><p>When we execute this command, we first parse the input parameters and then call <code>cmd.execute()</code>. This method will judge the input parameters and execute different codes for different commands.</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui&#x2F;src&#x2F;main.rsasync fn main() &#123;    #[cfg(windows)]    colored::control::set_virtual_terminal(true).unwrap();    let bin_name &#x3D; env!(&quot;CARGO_BIN_NAME&quot;);    let cmd: SuiCommand &#x3D; SuiCommand::parse();    ...    exit_main!(cmd.execute().await);&#125;</code></pre><p>If we use <code>sui genesis</code>.The <code>execute()</code> will match <code>SuiCommand::Genesis</code> .And the will call <code>genesis()</code>.</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui&#x2F;src&#x2F;sui_commands.rsimpl SuiCommand &#123;    pub async fn execute(self) -&gt; Result&lt;(), anyhow::Error&gt; &#123;        match self &#123;            ...            SuiCommand::Genesis &#123;                working_dir,                force,                from_config,                write_config,            &#125; &#x3D;&gt; genesis(from_config, write_config, working_dir, force).await,      ...   &#125;    &#125;&#125;</code></pre><p>This method mainly does 3 things:</p><ol><li>read config from disk</li><li>create some objects with those config value</li><li>store important objects(Nodeconfig,genesis_conf &amp; network_config) to disk</li></ol><h2 id="Read-config"><a href="#Read-config" class="headerlink" title="Read config"></a>Read config</h2><p>If we do not provide the config dir,It will create it with default parameter. </p><p>some default parameter:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-config&#x2F;src&#x2F;ib.rsconst SUI_DIR: &amp;str &#x3D; &quot;.sui&quot;;const SUI_CONFIG_DIR: &amp;str &#x3D; &quot;sui_config&quot;;pub const SUI_NETWORK_CONFIG: &amp;str &#x3D; &quot;network.yaml&quot;;pub const SUI_FULLNODE_CONFIG: &amp;str &#x3D; &quot;fullnode.yaml&quot;;pub const SUI_CLIENT_CONFIG: &amp;str &#x3D; &quot;client.yaml&quot;;pub const SUI_KEYSTORE_FILENAME: &amp;str &#x3D; &quot;sui.keystore&quot;;pub const SUI_GATEWAY_CONFIG: &amp;str &#x3D; &quot;gateway.yaml&quot;;pub const SUI_GENESIS_FILENAME: &amp;str &#x3D; &quot;genesis.blob&quot;;pub const SUI_DEV_NET_URL: &amp;str &#x3D; &quot;https:&#x2F;&#x2F;fullnode.devnet.sui.io:443&quot;;pub const AUTHORITIES_DB_NAME: &amp;str &#x3D; &quot;authorities_db&quot;;pub const CONSENSUS_DB_NAME: &amp;str &#x3D; &quot;consensus_db&quot;;pub const FULL_NODE_DB_PATH: &amp;str &#x3D; &quot;full_node_db&quot;;const DEFAULT_STAKE: StakeUnit &#x3D; 100000000000000;const DEFAULT_GAS_PRICE: u64 &#x3D; 1;const DEFAULT_COMMISSION_RATE: u64 &#x3D; 0;&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-config&#x2F;src&#x2F;genesis_config.rsconst DEFAULT_GAS_AMOUNT: u64 &#x3D; 100000000000000;const DEFAULT_NUMBER_OF_AUTHORITIES: usize &#x3D; 4;const DEFAULT_NUMBER_OF_ACCOUNT: usize &#x3D; 5;const DEFAULT_NUMBER_OF_OBJECT_PER_ACCOUNT: usize &#x3D; 5;</code></pre><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui&#x2F;src&#x2F;sui-commands.rsasync fn genesis(    from_config: Option&lt;PathBuf&gt;,    write_config: Option&lt;PathBuf&gt;,    working_dir: Option&lt;PathBuf&gt;,    force: bool,) -&gt; Result&lt;(), anyhow::Error&gt; &#123;    let sui_config_dir &#x3D; &amp;match working_dir &#123;        &#x2F;&#x2F; if a directory is specified, it must exist (it        &#x2F;&#x2F; will not be created)        Some(v) &#x3D;&gt; v,        &#x2F;&#x2F; create default Sui config dir if not specified        &#x2F;&#x2F; on the command line and if it does not exist        &#x2F;&#x2F; yet        None &#x3D;&gt; &#123;            let config_path &#x3D; sui_config_dir()?;            fs::create_dir_all(&amp;config_path)?;            config_path        &#125;    &#125;;     ...let client_path &#x3D; sui_config_dir.join(SUI_CLIENT_CONFIG);    let keystore_path &#x3D; sui_config_dir.join(SUI_KEYSTORE_FILENAME);    ...let network_path &#x3D; sui_config_dir.join(SUI_NETWORK_CONFIG);    let genesis_path &#x3D; sui_config_dir.join(SUI_GENESIS_FILENAME);    let mut genesis_conf &#x3D; match from_config &#123;        Some(path) &#x3D;&gt; PersistedConfig::read(&amp;path)?,        None &#x3D;&gt; &#123;            if keystore_path.exists() &#123;                let existing_keys &#x3D; FileBasedKeystore::new(&amp;keystore_path)?.addresses();                GenesisConfig::for_local_testing_with_addresses(existing_keys)            &#125; else &#123;                GenesisConfig::for_local_testing()            &#125;        &#125;    &#125;;...</code></pre><p><img src="/../img/301.png" alt="Untitled"></p><h2 id="create-objects"><a href="#create-objects" class="headerlink" title="create objects"></a>create objects</h2><p>Generate some important objects according the given configuration files or default configuration parameters.</p><ul><li>network_config</li><li>genesis_config</li><li>Client_config</li></ul><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui&#x2F;src&#x2F;sui_commands.rs--&gt;genesis()let mut network_config &#x3D; if let Some(validators) &#x3D; validator_info &#123;        ConfigBuilder::new(sui_config_dir)            .initial_accounts_config(genesis_conf)            .with_validators(validators)            .build()    &#125; else &#123;        ConfigBuilder::new(sui_config_dir)            .committee_size(NonZeroUsize::new(genesis_conf.committee_size).unwrap())            .initial_accounts_config(genesis_conf)&#x2F;&#x2F;will finally call build_with-calidators() in sui-config&#x2F;src&#x2F;builder.rs&#x2F;&#x2F;to genesis NetworkConfig&#123;&#125; and Nodeconfig            .build()    &#125;;</code></pre><p><img src="/../img/308.png" alt="Untitled"></p><h2 id="store-objects"><a href="#store-objects" class="headerlink" title="store objects"></a>store objects</h2><p>After all objects are generated, the <code>network_config , genesis, validator,keystore and client_config</code>  will be written to disk.</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">  let mut keystore &#x3D; FileBasedKeystore::new(&amp;keystore_path)?;  for key in &amp;network_config.account_keys &#123;      keystore.add_key(SuiKeyPair::Ed25519SuiKeyPair(key.copy()))?;  &#125;&#x2F;&#x2F;Take out the latest one in the keystore as active_address  let active_address &#x3D; keystore.addresses().pop();  network_config.genesis.save(&amp;genesis_path)?;  for validator in &amp;mut network_config.validator_configs &#123;      validator.genesis &#x3D; sui_config::node::Genesis::new_from_file(&amp;genesis_path);  &#125;  info!(&quot;Network genesis completed.&quot;);  network_config.save(&amp;network_path)?;  info!(&quot;Network config file is stored in &#123;:?&#125;.&quot;, network_path);  info!(&quot;Client keystore is stored in &#123;:?&#125;.&quot;, keystore_path);  let mut fullnode_config &#x3D; network_config.generate_fullnode_config();  fullnode_config.json_rpc_address &#x3D; sui_config::node::default_json_rpc_address();  fullnode_config.save(sui_config_dir.join(SUI_FULLNODE_CONFIG))?;  for (i, validator) in network_config      .into_validator_configs()      .into_iter()      .enumerate()  &#123;      let path &#x3D; sui_config_dir.join(format!(&quot;validator-config-&#123;&#125;.yaml&quot;, i));      validator.save(path)?;  &#125;  let mut client_config &#x3D; if client_path.exists() &#123;      PersistedConfig::read(&amp;client_path)?  &#125; else &#123;      SuiClientConfig::new(keystore.into())  &#125;;...  client_config.save(&amp;client_path)?;  info!(&quot;Client config file is stored in &#123;:?&#125;.&quot;, client_path);  Ok(())</code></pre><p>We can get the detail from the output log.</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">2022-12-13T08:12:04.072712Z  INFO sui_config::genesis_config: Creating accounts and gas objects...2022-12-13T08:16:33.039813Z  INFO sui::sui_commands: Network genesis completed.2022-12-13T08:16:33.069233Z  INFO sui::sui_commands: Network config file is stored in &quot;&#x2F;Users&#x2F;safeer&#x2F;.sui&#x2F;sui_config&#x2F;network.yaml&quot;.2022-12-13T08:16:33.069286Z  INFO sui::sui_commands: Client keystore is stored in &quot;&#x2F;Users&#x2F;safeer&#x2F;.sui&#x2F;sui_config&#x2F;sui.keystore&quot;.2022-12-13T08:18:02.582274Z  INFO sui::sui_commands: Client config file is stored in &quot;&#x2F;Users&#x2F;safeer&#x2F;.sui&#x2F;sui_config&#x2F;client.yaml&quot;</code></pre><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">➜  sui_config tree.├── client.yaml├── fullnode.yaml├── genesis.blob├── network.yaml├── sui.keystore├── validator-config-0.yaml├── validator-config-1.yaml├── validator-config-2.yaml└── validator-config-3.yaml</code></pre><h1 id="sui-start"><a href="#sui-start" class="headerlink" title="sui start"></a>sui start</h1><p>Run <code>sui start</code> to start the local Sui network with default location for configuration.</p><p>This command looks for the Sui network configuration file <code>network.yaml</code> in the <code>~/.sui/sui_config</code> directory. If there is no configuration in this directory, it will call genesis() to genesis the default configuration first.</p><p>The sui node is divided into validator and full node:</p><ul><li>validator: validators to focus on servicing and processing transactions. When a validator commits a new set of transactions (or a block of transactions), the validator pushes that block to all connected Full nodes that then service the queries from clients.</li><li>full node:Sui Full nodes validate blockchain activities, including transactions, checkpoints, and epoch changes. Each Full node stores and services the queries for the blockchain state and history.</li></ul><p>It will do those things:</p><ol><li>read configuration from disk(will call  genesis() to auto genesis if path is none)</li><li>build swarm with network_config(by default the swarm will contain 4 validators )</li><li>call swarm.launch() to start 4 validators.</li><li>run health Check service.</li></ol><h2 id="read-configuration"><a href="#read-configuration" class="headerlink" title="read configuration"></a>read configuration</h2><p>Before we used <code>sui genesis</code> to generate configuration files and perform storage persistence management.Here we will take it out, and then use these configuration files to generate <code>network_config</code> objects.</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui&#x2F;src&#x2F;sui_commands.rs--&gt;execute()SuiCommand::Start &#123;                config,                no_full_node,            &#125; &#x3D;&gt; &#123;              ... let network_config: NetworkConfig &#x3D; PersistedConfig::read(&amp;network_config_path)                    .map_err(|err| &#123;                        err.context(format!(                            &quot;Cannot open Sui network config file at &#123;:?&#125;&quot;,                            network_config_path                        ))                    &#125;)?;&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-config&#x2F;src&#x2F;lib.rsfn load&lt;P: AsRef&lt;Path&gt;&gt;(path: P) -&gt; Result&lt;Self, anyhow::Error&gt; &#123;        let path &#x3D; path.as_ref();        trace!(&quot;Reading config from &#123;&#125;&quot;, path.display());        let reader &#x3D; fs::File::open(path)            .with_context(|| format!(&quot;Unable to load config from &#123;&#125;&quot;, path.display()))?;        Ok(serde_yaml::from_reader(reader)?)    &#125;</code></pre><p><img src="/../img/302.png" alt="Untitled"></p><h2 id="build-swarm"><a href="#build-swarm" class="headerlink" title="build swarm"></a>build swarm</h2><p>Swarm is a handle to an in-memory Sui network.It contain a network_config and validators.</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-swarm&#x2F;src&#x2F;memory&#x2F;swarm.rs&#x2F;&#x2F;&#x2F; A handle to an in-memory Sui Network.#[derive(Debug)]pub struct Swarm &#123;    dir: SwarmDirectory,    network_config: NetworkConfig,    validators: HashMap&lt;SuiAddress, Node&gt;,    fullnodes: HashMap&lt;SuiAddress, Node&gt;,&#125;</code></pre><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui&#x2F;src&#x2F;sui_commands.rs--&gt;execute() let mut swarm &#x3D; if no_full_node &#123;                    Swarm::builder()                &#125; else &#123;                    Swarm::builder()                        .with_fullnode_rpc_addr(sui_config::node::default_json_rpc_address())                &#125;                .from_network_config(sui_config_dir()?, network_config);</code></pre><p><img src="/../img/303.png" alt="Untitled"></p><h2 id="run-validators"><a href="#run-validators" class="headerlink" title="run validators"></a>run validators</h2><p>After we build the swarm, we can use it to run each validator.</p><p>The whole process is:</p><p><code>//node.spawn()--&gt;Container::spawn()--&gt;SuiNode::start()</code></p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui&#x2F;src&#x2F;sui_commands.rs--&gt;execute() swarm.launch().await?;&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-swarm&#x2F;src&#x2F;memory&#x2F;swarm.rs&#x2F;&#x2F;&#x2F; Start all of the Validators associated with this Swarm    pub async fn launch(&amp;mut self) -&gt; Result&lt;()&gt; &#123;        let start_handles &#x3D; self            .nodes_iter_mut()            .map(|node| node.spawn())            .collect::&lt;Result&lt;Vec&lt;_&gt;&gt;&gt;()?;        try_join_all(start_handles).await?;        Ok(())    &#125;&#x2F;&#x2F;&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-swarm&#x2F;src&#x2F;memory&#x2F;node.rs&#x2F;&#x2F;&#x2F; Start this Node, returning a handle that will resolve when the node has completed starting    &#x2F;&#x2F;&#x2F; up.    pub fn spawn(&amp;mut self) -&gt; Result&lt;tokio::sync::oneshot::Receiver&lt;()&gt;&gt; &#123;        trace!(name &#x3D;% self.name(), &quot;starting in-memory node&quot;);        let (startup_receiver, node_handle) &#x3D;            Container::spawn(self.config.clone(), self.runtime_type);        self.thread &#x3D; Some(node_handle);        Ok(startup_receiver)    &#125;&#x2F;&#x2F;&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-swarm&#x2F;src&#x2F;memory&#x2F;container.rs--&gt;spawn()runtime.block_on(async move &#123;                let prometheus_registry &#x3D; metrics::start_prometheus_server(config.metrics_address);                info!(                    &quot;Started Prometheus HTTP endpoint. To query metrics use\n\tcurl -s http:&#x2F;&#x2F;&#123;&#125;&#x2F;metrics&quot;,                    config.metrics_address                );                let _server &#x3D; SuiNode::start(&amp;config, prometheus_registry).await.unwrap();                &#x2F;&#x2F; Notify that we&#39;ve successfully started the node                let _ &#x3D; startup_sender.send(());                &#x2F;&#x2F; run until canceled                cancel_reciever.map(|_| ()).await;</code></pre><h3 id="SuiNode"><a href="#SuiNode" class="headerlink" title="SuiNode"></a>SuiNode</h3><p>Suinode is the smallest unit of node management and is a very important object.It contains almost all the functions that a node needs to implement. The focus of our follow-up analysis is here.</p><p>The main members of it are:</p><ul><li>config</li><li>checkpoint_store</li><li>_batch_subsystem_handle</li><li>_execute_driver_handle</li><li>state</li><li>active</li><li>_p2p_network</li><li>_discovery</li><li>_state_sync</li><li>sim_node</li></ul><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-node&#x2F;src&#x2F;lib.rspub struct SuiNode &#123;    config: NodeConfig,    checkpoint_store: Arc&lt;CheckpointStore&gt;,    validator_server_info: Option&lt;ValidatorServerInfo&gt;,    _json_rpc_service: Option&lt;ServerHandle&gt;,    _batch_subsystem_handle: tokio::task::JoinHandle&lt;()&gt;,    _post_processing_subsystem_handle: Option&lt;tokio::task::JoinHandle&lt;Result&lt;()&gt;&gt;&gt;,    _gossip_handle: Option&lt;tokio::task::JoinHandle&lt;()&gt;&gt;,    _execute_driver_handle: tokio::task::JoinHandle&lt;()&gt;,    state: Arc&lt;AuthorityState&gt;,    active: Arc&lt;ActiveAuthority&lt;NetworkAuthorityClient&gt;&gt;,    transaction_orchestrator: Option&lt;Arc&lt;TransactiondOrchestrator&lt;NetworkAuthorityClient&gt;&gt;&gt;,    _prometheus_registry: Registry,    _p2p_network: anemo::Network,    _discovery: discovery::Handle,    _state_sync: state_sync::Handle,    reconfig_channel: (        tokio::sync::mpsc::Sender&lt;EpochId&gt;,        tokio::sync::mpsc::Receiver&lt;EpochId&gt;,    ),    #[cfg(msim)]    sim_node: sui_simulator::runtime::NodeHandle,&#125;</code></pre><p>Now let’s analyze the detailed steps in the node startup process:</p><ol><li>Judging whether it is fullnode or validator</li><li>Initialize DB(committee_store,checkpoint_store,state_sync_store,index_store,event_store)</li><li>create p2p network(discovery_handle,state_sync_handle)</li><li>create transaction_streamer (if it’s fullNode)</li><li>create node_sync_store</li><li>create handles(batch_subsystem_handle,post_processing_subsystem_handle,execute_driver_handle)</li><li>create json_rpc_service</li><li>finish start up</li></ol><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-node&#x2F;src&#x2F;lib.rspub async fn start(config: &amp;NodeConfig, prometheus_registry: Registry) -&gt; Result&lt;SuiNode&gt; &#123;        ...        let store &#x3D;            Arc::new(AuthorityStore::open(&amp;config.db_path().join(&quot;store&quot;), None, genesis).await?);        let committee_store &#x3D; Arc::new(CommitteeStore::new(            config.db_path().join(&quot;epochs&quot;),            &amp;committee,            None,        ));        let checkpoint_store &#x3D; CheckpointStore::new(&amp;config.db_path().join(&quot;checkpoints&quot;));        let state_sync_store &#x3D; RocksDbStore::new(            store.clone(),            committee_store.clone(),            checkpoint_store.clone(),        );        let index_store &#x3D; if is_validator &#123;            None        &#125; else &#123;            Some(Arc::new(IndexStore::open_tables_read_write(                config.db_path().join(&quot;indexes&quot;),                None,                None,            )))        &#125;;        let event_store &#x3D; if config.enable_event_processing &#123;            let path &#x3D; config.db_path().join(&quot;events.db&quot;);            let db &#x3D; SqlEventStore::new_from_file(&amp;path).await?;            db.initialize().await?;            Some(Arc::new(EventStoreType::SqlEventStore(db)))        &#125; else &#123;            None        &#125;;        let (p2p_network, discovery_handle, state_sync_handle) &#x3D;            Self::create_p2p_network(config, state_sync_store, &amp;prometheus_registry)?;       ...        let node_sync_store &#x3D; Arc::new(NodeSyncStore::open_tables_read_write(            config.db_path().join(&quot;node_sync_db&quot;),            None,            None,        ));        ...        let batch_subsystem_handle &#x3D; &#123;            &#x2F;&#x2F; Start batch system so that this node can be followed            let batch_state &#x3D; state.clone();            spawn_monitored_task!(async move &#123;                batch_state                    .run_batch_service(1000, Duration::from_secs(1))                    .await            &#125;)        &#125;;        let post_processing_subsystem_handle &#x3D;            if index_store.is_some() || config.enable_event_processing &#123;                let indexing_state &#x3D; state.clone();                Some(spawn_monitored_task!(async move &#123;                    indexing_state                        .run_tx_post_processing_process()                        .await                        .map_err(Into::into)                &#125;))            &#125; else &#123;                None            &#125;;                let execute_driver_handle &#x3D; active_authority.clone().spawn_execute_process().await;...        let json_rpc_service &#x3D; build_server(            state.clone(),            &amp;transaction_orchestrator.clone(),            config,            &amp;prometheus_registry,        )        .await?; ...        info!(&quot;SuiNode started!&quot;);        Ok(node)</code></pre><p><img src="/../img/304.png" alt="Untitled"></p><p>We can get more info from the debug output.(here we only focus on the startup information of one node: 0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a).</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">Launching: &#x2F;Users&#x2F;safeer&#x2F;sui&#x2F;target&#x2F;debug&#x2F;sui startLaunched process 801542022-12-14T08:59:58.765242Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_swarm::memory::container: Started Prometheus HTTP endpoint. To query metrics usecurl -s http:&#x2F;&#x2F;127.0.0.1:52424&#x2F;metrics2022-12-14T08:59:58.765242Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_swarm::memory::container: Started Prometheus HTTP endpoint. To query metrics use2022-12-14T09:00:12.433291Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_node: Initializing sui-node listening on &#x2F;ip4&#x2F;127.0.0.1&#x2F;tcp&#x2F;52420&#x2F;http node&#x3D;k#8c26601d82aac59fade53f345f8fe84239d69652142a7fdc7724f729a1716a07e16b07a91e5afd49ee45d5cc6f5f3a9e0d152797d9251f2be21811a764a2dea2539c3b7541d52bf2e78b4d256bd94daa193f0c2234ec76276b80c6f0fedf42742022-12-14T09:00:12.435263Z  WARN node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: mysten_metrics: init_metrics registry overwritten2022-12-14T09:13:51.573049Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Created&#x2F;opened SQLite EventStore on disk db_path&#x3D;&quot;&#x2F;Users&#x2F;safeer&#x2F;.sui&#x2F;sui_config&#x2F;authorities_db&#x2F;2727618050&#x2F;events.db&quot;2022-12-14T09:13:56.940186Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: SQLite events table is initialized with query &quot;CREATE TABLE IF NOT EXISTS events(timestamp INTEGER NOT NULL, seq_num INTEGER, event_num INTEGER, tx_digest BLOB, event_type INTEGER, package_id BLOB, module_name TEXT, function TEXT, object_type TEXT, object_id BLOB, fields TEXT, move_event_name TEXT, contents BLOB, sender BLOB, recipient TEXT);&quot;2022-12-14T09:14:05.392089Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;seq_num&quot;2022-12-14T09:14:10.311911Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;event_num&quot;2022-12-14T09:14:13.542209Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;timestamp&quot;2022-12-14T09:14:17.262100Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;tx_digest&quot;2022-12-14T09:14:20.959975Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;event_type&quot;2022-12-14T09:14:26.008174Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;package_id&quot;2022-12-14T09:14:30.079815Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;module_name&quot;2022-12-14T09:14:36.559934Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;sender&quot;2022-12-14T09:14:36.567273Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;recipient&quot;2022-12-14T09:14:36.574490Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;object_id&quot;2022-12-14T09:14:36.582455Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;object_type&quot;2022-12-14T09:14:36.589734Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_storage::event_store::sql: Index is ready column&#x3D;&quot;move_event_name&quot;2022-12-14T09:14:36.598456Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_node: P2p network started on 127.0.0.1:524222022-12-14T09:14:43.930692Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_core::authority_active: respawn_node_sync_process epoch&#x3D;02022-12-14T09:14:43.931350Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_core::authority_active: spawning node sync task2022-12-14T09:14:46.921729Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_json_rpc: acl&#x3D;Const(&quot;*&quot;)2022-12-14T09:14:46.922716Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_json_rpc: Sui JSON-RPC server listening on 0.0.0.0:9000 local_addr&#x3D;0.0.0.0:90002022-12-14T09:14:46.922772Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_json_rpc: Available JSON-RPC methods : [&quot;sui_moveCall&quot;, &quot;sui_getCoinMetadata&quot;, &quot;sui_publish&quot;, &quot;sui_getObjectsOwnedByAddress&quot;, &quot;sui_payAllSui&quot;, &quot;sui_transferObject&quot;, &quot;sui_unsubscribeEvent&quot;, &quot;sui_executeTransaction&quot;, &quot;sui_mergeCoins&quot;, &quot;sui_subscribeTransaction&quot;, &quot;sui_getEvents&quot;, &quot;sui_getObject&quot;, &quot;sui_getTransactionsInRange&quot;, &quot;sui_getObjectsOwnedByObject&quot;, &quot;sui_executeTransactionSerializedSig&quot;, &quot;sui_subscribeEvent&quot;, &quot;sui_paySui&quot;, &quot;sui_getRawObject&quot;, &quot;sui_splitCoin&quot;, &quot;sui_getNormalizedMoveFunction&quot;, &quot;sui_getTransaction&quot;, &quot;sui_getTotalTransactionNumber&quot;, &quot;sui_pay&quot;, &quot;sui_getTransactionAuthSigners&quot;, &quot;sui_getMoveFunctionArgTypes&quot;, &quot;sui_transferSui&quot;, &quot;sui_batchTransaction&quot;, &quot;sui_unsubscribeTransaction&quot;, &quot;sui_dryRunTransaction&quot;, &quot;sui_splitCoinEqual&quot;, &quot;sui_getNormalizedMoveModule&quot;, &quot;sui_getNormalizedMoveModulesByPackage&quot;, &quot;sui_tryGetPastObject&quot;, &quot;rpc.discover&quot;, &quot;sui_getTransactions&quot;, &quot;sui_getCommitteeInfo&quot;, &quot;sui_getNormalizedMoveStruct&quot;]2022-12-14T09:14:58.627411Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_node: SuiNode started!2022-12-14T09:15:08.957227Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;:connection-manager&#123;peer&#x3D;82a014fa&#125;: anemo::network::connection_manager: ConnectionManager started2022-12-14T09:15:08.957369Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_network::discovery: Discovery started2022-12-14T09:15:08.957515Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_network::state_sync: State-Synchronizer started2022-12-14T09:15:08.960911Z  INFO node&#123;name&#x3D;0x0f0711008959ccb02d47f2fb22c102bb60bcbe9a&#125;: sui_core::authority_active::execution_driver: Starting pending certificates execution process.</code></pre><h2 id="health-check"><a href="#health-check" class="headerlink" title="health check"></a>health check</h2><p>After starting the nodes,Performing a health check service to check that status of the node(Check every 5 seconds).</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;&#x2F;&#x2F;sui&#x2F;crates&#x2F;sui-swarm&#x2F;src&#x2F;memory&#x2F;node.rs--&gt;spawn()&#x2F;&#x2F;&#x2F; Perform a health check on this Node by:    &#x2F;&#x2F;&#x2F; * Checking that the node is running    &#x2F;&#x2F;&#x2F; * Calling the Node&#39;s gRPC Health service if it&#39;s a validator.    pub async fn health_check(&amp;self, is_validator: bool) -&gt; Result&lt;(), HealthCheckError&gt; &#123;        let thread &#x3D; self.thread.as_ref().ok_or(HealthCheckError::NotRunning)?;        if !thread.is_alive() &#123;            return Err(HealthCheckError::NotRunning);        &#125;        if is_validator &#123;            let channel &#x3D; mysten_network::client::connect(self.config.network_address())                .await                .map_err(|err| anyhow!(err.to_string()))                .map_err(HealthCheckError::Failure)                .tap_err(|e| error!(&quot;error connecting to &#123;&#125;: &#123;e&#125;&quot;, self.name()))?;            let mut client &#x3D; tonic_health::proto::health_client::HealthClient::new(channel);            client                .check(tonic_health::proto::HealthCheckRequest::default())                .await                .map_err(|e| HealthCheckError::Failure(e.into()))                .tap_err(|e| error!(&quot;error performing health check on &#123;&#125;: &#123;e&#125;&quot;, self.name()))?;        &#125;        Ok(())    &#125;</code></pre><h1 id="Summarize"><a href="#Summarize" class="headerlink" title="Summarize"></a><strong>Summarize</strong></h1><p>We analyzed the startup process of the node in this page. </p><ul><li>reading the configuration file</li><li>generating objects according to the configuration file</li><li>open services during the node startup process</li></ul><p>In the following articles, we will analyze each part in detail, the most important of which is to analyze the various services opened during the node startup process(SuiNode object).</p>]]></content>
      
      
      
        <tags>
            
            <tag> Sui blockchain,Sui,Move Language </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Sui Concept and Testnet</title>
      <link href="//post/Sui%20Concept%20and%20Testnet.html"/>
      <url>//post/Sui%20Concept%20and%20Testnet.html</url>
      
        <content type="html"><![CDATA[<p>We can learn Sui concepts from here:<br><a href="https://docs.sui.io/learn/how-sui-works">https://docs.sui.io/learn/how-sui-works</a></p><h1 id="SUI-CODE-ARCHITECTURE"><a href="#SUI-CODE-ARCHITECTURE" class="headerlink" title="SUI CODE ARCHITECTURE"></a>SUI CODE ARCHITECTURE</h1><p>The latest version of the code  is <code>devnet-0.17.0</code></p><pre class="line-numbers language-jsx" data-language="jsx"><code class="language-jsx">git clone https:&#x2F;&#x2F;github.com&#x2F;MystenLabs&#x2F;sui.git</code></pre><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">.├── CODE_OF_CONDUCT.MD├── CONTRIBUTING.md├── Cargo.lock├── Cargo.toml├── DEVX_ROADMAP.md├── ISSUES.md├── LICENSE├── LICENSE-docs├── README.md├── RELEASES.md├── SECURITY.md├── apps│   ├── core│   ├── explorer│   └── wallet├── crates│   ├── component│   ├── mysten-metrics│   ├── mysten-network│   ├── mysten-util-mem│   ├── mysten-util-mem-derive│   ├── prometheus-closure-metric│   ├── rccheck│   ├── sui│   ├── sui-adapter│   ├── sui-adapter-transactional-tests│   ├── sui-benchmark│   ├── sui-cluster-test│   ├── sui-config│   ├── sui-core│   ├── sui-cost│   ├── sui-cost-tables│   ├── sui-faucet│   ├── sui-framework│   ├── sui-framework-build│   ├── sui-indexer│   ├── sui-json│   ├── sui-json-rpc│   ├── sui-json-rpc-types│   ├── sui-keys│   ├── sui-macros│   ├── sui-network│   ├── sui-node│   ├── sui-open-rpc│   ├── sui-open-rpc-macros│   ├── sui-rosetta│   ├── sui-sdk│   ├── sui-simulator│   ├── sui-source-validation│   ├── sui-storage│   ├── sui-swarm│   ├── sui-telemetry│   ├── sui-test-validator│   ├── sui-tool│   ├── sui-transaction-builder│   ├── sui-transactional-test-runner│   ├── sui-types│   ├── sui-verifier│   ├── sui-verifier-transactional-tests│   ├── telemetry-subscribers│   ├── test-utils│   ├── typed-store│   ├── typed-store-derive│   ├── workspace-hack│   └── x├── dapps│   └── README.md├── deny.toml├── doc│   ├── book│   ├── code-examples│   ├── locals│   ├── paper│   ├── src│   ├── static│   ├── template│   ├── tips│   └── utils├── docker│   ├── fullnode│   ├── sui-node│   └── sui-tools├── narwhal│   ├── CONTRIBUTING.md│   ├── Docker│   ├── README.md│   ├── benchmark│   ├── config│   ├── consensus│   ├── crypto│   ├── dag│   ├── deny.toml│   ├── examples│   ├── executor│   ├── network│   ├── node│   ├── primary│   ├── scripts│   ├── storage│   ├── test-utils│   ├── types│   └── worker├── package.json├── pnpm-lock.yaml├── pnpm-workspace.yaml├── rust-toolchain├── rustfmt.toml├── scripts│   ├── bench_sweep.py│   ├── changed-files.sh│   ├── dependency.py│   ├── simtest│   ├── update_fastcrypto.sh│   └── update_narwhal.sh├── sdk│   ├── bcs│   ├── typescript│   └── wallet-adapter├── sui_programmability│   └── examples└── target    ├── CACHEDIR.TAG    ├── debug    └── tmp</code></pre><p>As we can see, the structure of the code is similar to Aptos.</p><p>Different functions are organized in different packages in <code>sui/crates/ folder</code>.The most parts are those:</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">├── crates│   ├── sui│   ├── sui-config│   ├── sui-core│   ├── sui-faucet│   ├── sui-keys│   ├── sui-macros│   ├── sui-network│   ├── sui-node</code></pre><h2 id="cartes-x2F-sui"><a href="#cartes-x2F-sui" class="headerlink" title="cartes&#x2F;sui"></a>cartes&#x2F;sui</h2><p>Like Aptos, the Sui is a CLI tool. we can use it to create a testnet node or interact with sui network. The endpoint of code is in <code>crates/src/main.rc —&gt; fn main()</code>. Our entire code analysis process starts with this entry.</p><p><img src="/../img/123.png" alt="Untitled"></p><h2 id="other-packages"><a href="#other-packages" class="headerlink" title="other packages"></a>other packages</h2><ul><li>sui-faucet - sui faucet related service.</li><li>sui-node - node related</li><li>sui-core - authority server and Sui Gateway</li><li>sui-types - coins, gas, and other object types</li><li>sui-network - networking interfaces</li></ul><h1 id="Build-Sui-and-run-local-testnet"><a href="#Build-Sui-and-run-local-testnet" class="headerlink" title="Build Sui and run local testnet"></a>Build Sui and run local testnet</h1><p>The Sui repository includes two primary branches, <code>devnet</code> and <code>main</code>.</p><ul><li>The <code>devnet</code> branch includes the latest stable build of Sui. Choose the <code>devnet</code> branch if you want to build or test on Sui. If you encounter an issue or find a bug, it may already be fixed in the <code>main</code> branch. To submit a pull request (PR), you should push commits to your fork of the <code>main</code> branch.</li><li>The <code>main</code> branch includes the most recent changes and updates. Use the <code>main</code> branch if you want to contribute to the Sui project. The <code>main</code> branch may include unreleased changes, or introduce changes that cause issues in apps created using an earlier version.</li></ul><blockquote><p><em>The previous</em> <code>git clone</code> <em>command syncs with the</em> <code>devnet</code> <em>branch, which makes sure the source code is compatible with our Devnet. If you want to run a network locally using the latest version and don’t need to interact with our Devnet, you should switch to</em> <code>main</code> <em>branch.</em></p></blockquote><h2 id="Build"><a href="#Build" class="headerlink" title="Build"></a><strong>Build</strong></h2><p>Cause the development team did not provide a compiled version,so we have to build Sui by ourself. We can use this command to build.</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">cargo install --locked --git https:&#x2F;&#x2F;github.com&#x2F;MystenLabs&#x2F;sui.git --branch devnet sui</code></pre><p>This will take a long time and may fail with an error.If there is any error causing the failure.That is probably due to environmental problems(the version of the compilation tool is too low).Please update all the tools required and retry.If you still can’t compile, then change the system.</p><p>I have been unable to compile successfully using macos 10.14.1 before, and I successed after I changed it to 12.6.</p><p>It took about an hour to compile this.</p><p><img src="/../img/1234.png" alt="Untitled"></p><blockquote><p>For more detail : <a href="https://docs.sui.io/build/install">https://docs.sui.io/build/install</a></p></blockquote><p>After we compiled we can see the executable file in <code>~/.carge/bin/sui</code>. And the we can use it to run local testnet.</p><p><img src="/../img/12345.png" alt="Untitled"></p><h2 id="genesis"><a href="#genesis" class="headerlink" title="genesis"></a>genesis</h2><p>To create the configuration files and objects for a local Sui network, run the <code>genesis</code> command. Genesis creates the network configuration files in the <code>~/.sui/sui_config</code> folder. </p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">#genesis sui genesis#output2022-12-06T08:54:58.103638Z  INFO sui_config::genesis_config: Creating accounts and gas objects...2022-12-06T08:54:58.190260Z  INFO sui::sui_commands: Network genesis completed.2022-12-06T08:54:58.193319Z  INFO sui::sui_commands: Network config file is stored in &quot;&#x2F;Users&#x2F;safeer&#x2F;.sui&#x2F;sui_config&#x2F;network.yaml&quot;.2022-12-06T08:54:58.193344Z  INFO sui::sui_commands: Client keystore is stored in &quot;&#x2F;Users&#x2F;safeer&#x2F;.sui&#x2F;sui_config&#x2F;sui.keystore&quot;.2022-12-06T08:54:58.198162Z  INFO sui::sui_commands: Client config file is stored in &quot;&#x2F;Users&#x2F;safeer&#x2F;.sui&#x2F;sui_config&#x2F;client.yaml&quot;.</code></pre><p>This includes a YAML file for fullnode, network, client, and each validator. It also creates a sui.keystore that stores client key pairs.</p><p>The network that genesis creates includes four validators and five user accounts that contain five coin objects each.</p><p><img src="/../img/123456.png" alt="Untitled"></p><h2 id="Start-the-local-network"><a href="#Start-the-local-network" class="headerlink" title="Start the local network"></a>Start the local network</h2><p>Run the following command to start the local Sui network with default configuration( <code>~/.sui/sui_config/network.yaml</code>).</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sui start</code></pre><p><img src="/../img/23.jpeg" alt="Untitled"></p><p>When you start the network, Sui generates an <code>authorities_db directory</code> that stores validator data, and a <code>consensus_db</code> directory that stores consensus data.</p><p><img src="/../img/24.png" alt="Untitled"></p><h1 id="Interact-with-Sui-local-network"><a href="#Interact-with-Sui-local-network" class="headerlink" title="Interact with Sui local network"></a>Interact with Sui local network</h1><h2 id="sui-client"><a href="#sui-client" class="headerlink" title="sui client"></a>sui client</h2><p>we can use Sui CLI to interact with Sui net</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">#use -h to show help of sui clisui -h   # use subcommand helpsui client -h# use interactive shell#The console command looks for the client configuration file client.yaml in the ~&#x2F;.sui&#x2F;sui_config directorysui console </code></pre><p><img src="/../img/25.png" alt="Untitled"></p><p>Some example:</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">#the list of account addresses available on the platformsui client addresses#Sui sets a default address to use for commands. It uses the active address for commands that require an address.sui client active-address#You can call the objects command with or without specifying an address. Sui uses the active address if you do not specify one.sui client objects#create a new addresssui client new-address ed25519# send a trabsfersui client transfer --to 0x49450a4a3cac01a44be9e82fe1b29df5a24bd244 --object-id 0xcc054f9b68501541c4a5355920fb896b1765a889 --gas-budget 100</code></pre><p><img src="/../img/26.png" alt="Untitled"></p><p><img src="/../img/27.png" alt="Untitled"></p><p><img src="/../img/28.png" alt="Untitled"></p><h1 id="Debug-Sui-with-vscode"><a href="#Debug-Sui-with-vscode" class="headerlink" title="Debug Sui with vscode"></a>Debug Sui with vscode</h1><p>In the process of analyzing code, debugging is an essential step.It can help us understand the logic and flow during code execution. It can also help us find bugs in the code.</p><p>Steps(on mac):</p><ol><li>install vscode and plug-in (rust-analyzer,CodeLLDB)</li><li>open the sui folder</li><li>open the target file</li></ol><p>create new launch.json file(will be automatically generated  ).</p><p><img src="/../img/30.png" alt="Untitled"></p><p>add args:</p><p><img src="/../img/31.png" alt="Untitled"></p><p>Start to debug:</p><p><img src="/../img/33.png" alt="Untitled"></p><p><strong>About MoveBit</strong></p><p><strong>MoveBit is a blockchain security company focused on the Move Ecosystem by pioneering the use of cutting-edge Formal Verification. The team consists of security professionals from academia and enterprise with 10 years of security experience. they were one of the earliest contributors to the Move ecosystem, working with Move developers to set the standard for secure Move applications and make the Move ecosystem the most secure Web3 destination.</strong></p><p>Twitter: <a href="https://twitter.com/MoveBit">https://twitter.com/MoveBit</a>_<br>Medium: <a href="https://movebit.medium.com/">https://movebit.medium.com/</a><br>Github: <a href="https://github.com/movebit">https://github.com/movebit</a><br>Discord: <a href="https://discord.com/invite/7wM8VU9Gyj">https://discord.com/invite/7wM8VU9Gyj</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> SuiEcosystem, MoveLang </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>The First MoveCTF Summary</title>
      <link href="//post/the-first-movectf-summary.html"/>
      <url>//post/the-first-movectf-summary.html</url>
      
        <content type="html"><![CDATA[<p>MoveCTF  (Nov 5 — Nov 7 &#x2F; 2022), the first Move CTF (Capture The Flag) security competition, exclusively sponsored by Sui Network development company Mysten Labs, concluded successfully. The competition was organized by MoveBit, ChainFlag, OtterSec and co-organized by Mynft and SuiWorld DAO.</p><p>By the end of the competition (November 7, 2022 at 6:00 PST), there were 1,280 registered competitors from 25 countries and regions worldwide. The competition had four challenges: checkin, simple game, flash loan, and move lock. A total of 18 people solved all four challenges.</p><p>Each challenge was completed as follows.</p><ul><li>checkin 305 solves </li><li>simple game 22 solves</li><li>move lock 37 solves</li><li>flash loan 41 solves</li></ul><p><img src="/../img/post3_1.png" alt="alt rank"></p><p>The top three winners were neo, Ekipa Remontowa, and Slss. Congrats to these three winners, who will receive an exclusive prize sponsored by Sui Network and a special NFT. The remaining participants who completed at least one challenge will receive a Sui net-based POAP NFT.</p><p>In addition, the MoveCTF was well received by Move language learners, and we will soon open four challenges for the Move community to learn and use.</p><p><img src="/../img/post3_2.png" alt="alt rank"></p><p><img src="/../img/post3_3.png" alt="alt rank"></p><p>Thanks to all the participants and partners, let’s look forward to the next Move CTF!</p><p>MoveCTF official website:<br><a href="http://movectf.movebit.xyz/">http://movectf.movebit.xyz/</a></p><p>MoveBit official website:<br><a href="https://www.movebit.xyz/">https://www.movebit.xyz/</a></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>MoveBit Launches the First Move CTF Security Competition Sponsored by Mysten Labs</title>
      <link href="//post/movectf.html"/>
      <url>//post/movectf.html</url>
      
        <content type="html"><![CDATA[<p>The Move ecosystem is booming, and with it comes a need for Move-specific security expertise. Though Move is a secure and safe language by design, Move developers still need a place to learn and practice writing secure Move smart contracts.</p><p>The Move community is thus organizing a flagship Move security competition to attract more security professionals and developers interested in the Move language and the Move ecosystem. </p><p>MoveBit is collaborating with many Move ecosystem projects to organize the first ever Move ecosystem CTF (Capture The Flag) security competition: MoveCTF. Mysten Labs (Sui) will be a supporter and sole sponsor of the competition.</p><p><img src="/../img/movectf.jpeg" alt="MoveCTF"></p><p>MoveCTF’s first Move security competition will be held online. The competition will take place from 6pm on November 5 to 6pm on November 7 (PST), for a total of 48 hours. This MoveCTF competition will set four security challenges related to Move language, with a range of difficulty levels. Participants will earn points for completing each challenge. The top four fastest participants for each challenge will be rewarded with additional points.</p><p>The top three winners ranked by total points will be awarded 1000 USD, 500 USD, and 300 USD respectively, sponsored by Mysten Labs (Sui). In addition, MoveCTF partner Mynft will mint a mystery NFT for each of the three winners on Suia. Mynft will also mint a Move Hacker NFT for each participant who completes at least one Move security challenge.</p><p>Registration for the MoveCTF competition is now open. For more information, please visit this link:  <a href="https://movectf.movebit.xyz/">https://movectf.movebit.xyz/</a></p><h2 id="About-MoveBit"><a href="#About-MoveBit" class="headerlink" title="About MoveBit"></a>About MoveBit</h2><p>MoveBit is a security audit company for the Move ecosystem with a vision to make Move’s ecosystem the most secure Web3 destination. The MoveBit team is composed of academic security experts and corporate security leaders with 10 years of security experience. The team was one of the earliest contributors to the Move ecosystem, and works with Move developers to formulate the standards for secure Move applications.</p><p><img src="/../img/movebit_logo.png" alt="MoveBit"></p><h2 id="About-Mysten-Labs"><a href="#About-Mysten-Labs" class="headerlink" title="About Mysten Labs"></a>About Mysten Labs</h2><p>Mysten Labs develops tools that make Web3 secure, reliable, and ready for mass adoption. It creates foundational infrastructure to accelerate the adoption of Web3. The company gathers the brightest minds to create infrastructure and tools for a decentralized future for everyone.</p><p><img src="/../img/sui_logo.png" alt="MoveBit"></p><p>Sui by Mysten Labs is a next-generation smart contract platform with high throughput and low latency, an asset-oriented programming model powered by Move. It is designed from the ground up to enable creators and developers to build Web3 applications that could cater to billions of users.</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Verify Smart Contracts in Aptos with the Move Prover Pt.1</title>
      <link href="//post/move-prover-tutorial-part-1.html"/>
      <url>//post/move-prover-tutorial-part-1.html</url>
      
        <content type="html"><![CDATA[<p><em>Xudong Wang  &amp; 0xyi</em> 09-14-2022</p><p>It has been widely acclaimed that the Move Prover makes it very simple to formally verify Move contracts, offering very strong safety guarantees.  How does it work? Does it provide the ultimate safety guarantee for smart contracts?</p><p>We will write an article series on how to verify smart contracts with the Move Prover in Aptos, what are the tips and best practices, a more complicated case study using the Move Prover, and what are the current limitation and its future outlook. </p><p>For this part, we will first give a tutorial on how to verify smart contracts with the Move Prover in Aptos.</p><h2 id="What-is-the-Move-Prover"><a href="#What-is-the-Move-Prover" class="headerlink" title="What is the Move Prover"></a>What is the Move Prover</h2><p>Formal verification is a technique that uses rigorous mathematical methods to describe the behavior and reason about the correctness of computer systems.<br>Now it has certain applications in the fields of operating systems, compilers, and other fields that require high correctness.</p><p>Smart contracts deployed on the blockchain manipulate various digital assets, and their correctness is also critical.<br><strong>Move Prover (MVP)</strong> is designed to prevent bugs in smart contracts written in the Move language.</p><p>In Aptos ( and Starcoin&#x2F;Sui ), Users can specify functional properties of smart contracts using the <strong>Move Specification Language (MSL)</strong>, and then use the Move Prover to automatically and statically inspect them.</p><p>Simply put, there can be two components in a Move file:</p><ul><li><p>Part of it is program code, which is the part most of us are most familiar with. It is written in the Move programming language (sometimes just called the Move language). We use it to define data types, and functions.</p></li><li><p>The other part is the Formal specification. It is optional and written in the Move specification language. We use it to describe what properties a program code should satisfy. Such as describing the behavior of a function.</p></li></ul><p>When we write the formal specification, after calling the Move Prover, it will verify whether the Move program meets these requirements according to the written specification, helping developers to find potential problems as early as possible in the development stage, and giving other users confidence in the properties of the program that has been verified.</p><h2 id="Install-Prover-dependencies-in-Aptos"><a href="#Install-Prover-dependencies-in-Aptos" class="headerlink" title="Install Prover dependencies in Aptos"></a>Install Prover dependencies in Aptos</h2><p>Before using the Move Prover, let’s install some of its external dependencies.<br>It is assumed that you already have <a href="https://aptos.dev/cli-tools/aptos-cli-tool/install-aptos-cli">installed Aptos CLI</a>.<br>Make sure you did “Step 3” and installed the dependencies of the Move Prover:</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">.&#x2F;scripts&#x2F;dev_setup.sh -yp. ~&#x2F;.profile</code></pre><p>When the above command is executed, enter <code>boogie /version</code>, if the output is similar to “Boogie program verifier version X.X.X”, then the installation has been successful.</p><p>Note that currently the Move Prover can only run under UNIX-based operating systems (such as Linux, and macOS).<br>Windows users can run it by installing <a href="https://docs.microsoft.com/en-us/windows/wsl/install">WSL</a>.</p><h2 id="Prepare-an-example-for-verification-in"><a href="#Prepare-an-example-for-verification-in" class="headerlink" title="Prepare an example for verification in"></a>Prepare an example for verification in</h2><h3 id="Project-creation"><a href="#Project-creation" class="headerlink" title="Project creation"></a>Project creation</h3><p>First, let’s create a new empty Move package:</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">mkdir basic_coincd basic_coinaptos move init --name BasicCoin</code></pre><p>You can see that its directory structure is as follows:</p><pre class="line-numbers language-none"><code class="language-none">basic_coin    |    |---- Move.toml (text file)    |    &#96;---- sources   (Directory)</code></pre><h3 id="Module-code"><a href="#Module-code" class="headerlink" title="Module code"></a>Module code</h3><p>Now create <code>basic_coin/sources/BasicCoin.move</code>.</p><details><summary> BasicCoin.move content</summary><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">&#x2F;&#x2F;&#x2F; This module defines a minimal and generic Coin and Balance.module BasicCoin::basic_coin &#123;    use std::error;    use std::signer;    &#x2F;&#x2F;&#x2F; Error codes    const ENOT_MODULE_OWNER: u64 &#x3D; 0;    const EINSUFFICIENT_BALANCE: u64 &#x3D; 1;    const EALREADY_HAS_BALANCE: u64 &#x3D; 2;    struct Coin&lt;phantom CoinType&gt; has store &#123;        value: u64    &#125;    struct Balance&lt;phantom CoinType&gt; has key &#123;        coin: Coin&lt;CoinType&gt;    &#125;    &#x2F;&#x2F;&#x2F; Publish an empty balance resource under &#96;account&#96;&#39;s address. This function must be called before    &#x2F;&#x2F;&#x2F; minting or transferring to the account.    public fun publish_balance&lt;CoinType&gt;(account: &amp;signer) &#123;        let empty_coin &#x3D; Coin&lt;CoinType&gt; &#123; value: 0 &#125;;        assert!(!exists&lt;Balance&lt;CoinType&gt;&gt;(signer::address_of(account)), error::already_exists(EALREADY_HAS_BALANCE));        move_to(account, Balance&lt;CoinType&gt; &#123; coin:  empty_coin &#125;);    &#125;    &#x2F;&#x2F;&#x2F; Mint &#96;amount&#96; tokens to &#96;mint_addr&#96;. This method requires a witness with &#96;CoinType&#96; so that the    &#x2F;&#x2F;&#x2F; module that owns &#96;CoinType&#96; can decide the minting policy.    public fun mint&lt;CoinType: drop&gt;(mint_addr: address, amount: u64, _witness: CoinType) acquires Balance &#123;        &#x2F;&#x2F; Deposit &#96;total_value&#96; amount of tokens to mint_addr&#39;s balance        deposit(mint_addr, Coin&lt;CoinType&gt; &#123; value: amount &#125;);    &#125;    public fun balance_of&lt;CoinType&gt;(owner: address): u64 acquires Balance &#123;        borrow_global&lt;Balance&lt;CoinType&gt;&gt;(owner).coin.value    &#125;    &#x2F;&#x2F;&#x2F; Transfers &#96;amount&#96; of tokens from &#96;from&#96; to &#96;to&#96;. This method requires a witness with &#96;CoinType&#96; so that the    &#x2F;&#x2F;&#x2F; module that owns &#96;CoinType&#96; can  decide the transferring policy.    public fun transfer&lt;CoinType: drop&gt;(from: &amp;signer, to: address, amount: u64, _witness: CoinType) acquires Balance &#123;        let addr_from &#x3D; signer::address_of(from);        let check &#x3D; withdraw&lt;CoinType&gt;(addr_from, amount);        deposit&lt;CoinType&gt;(to, check);    &#125;    fun withdraw&lt;CoinType&gt;(addr: address, amount: u64) : Coin&lt;CoinType&gt; acquires Balance &#123;        let balance &#x3D; balance_of&lt;CoinType&gt;(addr);        assert!(balance &gt;&#x3D; amount, EINSUFFICIENT_BALANCE);        let balance_ref &#x3D; &amp;mut borrow_global_mut&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;        *balance_ref &#x3D; balance - amount;        Coin&lt;CoinType&gt; &#123; value: amount &#125;    &#125;        fun deposit&lt;CoinType&gt;(addr: address, check: Coin&lt;CoinType&gt;) acquires Balance&#123;        let balance &#x3D; balance_of&lt;CoinType&gt;(addr);        let balance_ref &#x3D; &amp;mut borrow_global_mut&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;        let Coin &#123; value &#125; &#x3D; check;        *balance_ref &#x3D; balance + value;    &#125;&#125;</code></pre></details><p>Here we assume that you have a certain grasp of the Move language, and can understand the source code of <code>BasicCoin.move</code> above and know the function of each part.</p><h3 id="TOML-configuration"><a href="#TOML-configuration" class="headerlink" title="TOML configuration"></a>TOML configuration</h3><p>BasicCoin uses some facilities of the Aptos standard library, and also needs to add <code>aptos-framework</code> to the dependencies. We also need to specify what numerical address it should be replaced with. </p><p>Therefore, we modify Move.toml as follows:</p><pre class="line-numbers language-toml" data-language="toml"><code class="language-toml">[package]name &#x3D; &quot;BasicCoin&quot;version &#x3D; &quot;0.0.0&quot;[dependencies]AptosFramework &#x3D; &#123; git &#x3D; &quot;https:&#x2F;&#x2F;github.com&#x2F;aptos-labs&#x2F;aptos-core.git&quot;, subdir &#x3D; &quot;aptos-move&#x2F;framework&#x2F;aptos-framework&#x2F;&quot;, rev &#x3D; &quot;main&quot; &#125;[addresses]BasicCoin&#x3D;&quot;Replace_It_With_Your_Numerical_Address&quot;  </code></pre><h2 id="The-first-verification-code"><a href="#The-first-verification-code" class="headerlink" title="The first verification code"></a>The first verification code</h2><p>To give us a first impression of the use of the Move Prover, add the following code snippet to BasicCoin.move:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec balance_of &#123;    pragma aborts_if_is_strict;&#125;</code></pre><p>Syntactically, this code can be added anywhere in the BasicCoin module, but it is recommended to place it after the definition of the <code>balance_of</code> function in order to clearly see the correspondence between the definition and the specification when reading the code.</p><p>Simply put, the <code>spec balance_of &#123;...&#125;</code> block will contain our <strong>property specification</strong> for the <code>balance_of</code> function.<br>There are many types of property specifications, some common examples are:</p><ul><li>Will this function abort? Under what circumstances does it abort?</li><li>What conditions must be met for the parameters to call this function?</li><li>What is the return value of this function?</li><li>After the function is executed, how will the state of the virtual machine be changed?</li><li>What invariants does this function maintain?</li></ul><p>For example, the Move Prover allows all possible aborts by default when we don’t give any abort conditions.<br>And in the simple snippet above, we tell Prover with the directive <code>aborts_if_is_strict</code>:</p><blockquote><p>I would like to strictly check the possibility of aborting this function. Report an error if there is any abort not listed by the programmer.</p></blockquote><p>Now, we run the <code>prove</code> command in the <code>basic_coin</code> directory:</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">aptos move prove</code></pre><p>it will call the Move Prover to check the code in the package.<br>Then we can see the Prover reporting the following error message:</p><pre class="line-numbers language-none"><code class="language-none">error: abort not covered by any of the &#96;aborts_if&#96; clauses   ┌─ .&#x2F;sources&#x2F;BasicCoin.move:38:5   │35 │           borrow_global&lt;Balance&lt;CoinType&gt;&gt;(owner).coin.value   │           ------------- abort happened here with execution failure   ·38 │ ╭     spec balance_of &#123;39 │ │       pragma aborts_if_is_strict;40 │ │     &#125;   │ ╰─────^   │   &#x3D;     at .&#x2F;sources&#x2F;BasicCoin.move:34: balance_of   &#x3D;         owner &#x3D; 0x29   &#x3D;     at .&#x2F;sources&#x2F;BasicCoin.move:35: balance_of   &#x3D;         ABORTEDFAILURE proving 1 modules from package &#96;basic_coin&#96; in 1.794s&#123;  &quot;Error&quot;: &quot;Move Prover failed: exiting with verification errors&quot;&#125;</code></pre><p>Prover’s output tells us that it found a situation where the <code>balance_of</code> function aborts, but we don’t explicitly point out the possibility of such aborts.<br>Looking at the code that triggers the abort, we can see that the exception is caused by calling the built-in <code>borrow_global</code> function when the <code>owner</code> does not own a resource of type <code>Balance&lt;CoinType&gt;</code>.<br>Following the guidance of the error message, we can add the following <code>aborts_if</code> condition:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec balance_of &#123;    pragma aborts_if_is_strict;    aborts_if !exists&lt;Balance&lt;CoinType&gt;&gt;(owner);&#125;</code></pre><p>After adding this condition, try calling Prover again and see that there are no more validation errors.<br>Now we can confidently confirm that the <code>balance_of</code> function has one and only one possibility of abnormal termination, that is, the parameter <code>owner</code> does not own a resource of type <code>Balance&lt;CoinType&gt;</code>.</p><h2 id="Verify-withdraw-function"><a href="#Verify-withdraw-function" class="headerlink" title="Verify withdraw function"></a>Verify withdraw function</h2><p>The signature of the function <code>withdraw</code> is as follows:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">fun withdraw&lt;CoinType&gt;(addr: address, amount: u64) : Coin&lt;CoinType&gt; acquires Balance</code></pre><p>Its role is to withdraw the <code>amount</code> of coins from the address <code>addr</code> and return it.</p><h3 id="Specify-the-abort-condition-for-withdraw"><a href="#Specify-the-abort-condition-for-withdraw" class="headerlink" title="Specify the abort condition for withdraw"></a>Specify the abort condition for <code>withdraw</code></h3><p>There are two possibilities for <code>withdraw</code> to abort:</p><ol><li>No resource of type <code>Balance&lt;CoinType&gt;</code> in <code>addr</code>.</li><li>The balance in <code>addr</code> is less than <code>amount</code>.</li></ol><p>From these, we can define the abort condition like this:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec withdraw &#123;    let balance &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    aborts_if !exists&lt;Balance&lt;CoinType&gt;&gt;(addr);    aborts_if balance &lt; amount;&#125;</code></pre><ul><li>A spec block can contain let bindings, which bind a long expression with a name that can be used repeatedly.<br><code>global&lt;T&gt;(addr): T</code> is a built-in function that returns a resource of type <code>T</code> at address <code>addr</code>.<br>Here, we set balance to the number of tokens owned by addr via the let binding;</li><li><code>exists&lt;T&gt;(address): bool</code> is a built-in function that returns true if resource <code>T</code> exists at address <code>addr</code>; otherwise returns false.</li></ul><p>The two lines of <code>aborts_if</code> statements correspond to the two conditions mentioned above.<br>In general, if a function has multiple <code>aborts_if</code> conditions, the conditions are ORed together.</p><p>As mentioned earlier, if we don’t specify any abort conditions, Prover will not impose any restrictions on aborts.<br>But once we give any kind of abort conditions, Prover defaults that we want to strictly check all possible aborts, so we need to list all possible conditions, which is equivalent to implicitly adding the instruction <code>pragma aborts_if_is_strict</code>.<br>If only some of the conditions for abnormal exit are listed, the Prover will report a validation error.<br>However, if the <code>pragma aborts_if_is_partial</code> is defined in the spec block, this is equivalent to telling the Prover:</p><blockquote><p>I just want to list some of the conditions that will cause an abort, please just verify that it will abort under those conditions.</p></blockquote><p>If you are interested, you can do such a set of experiments to verify:</p><ul><li>When deleting any of the above two <code>aborts_if</code> conditions, Prover will report an error;</li><li>When all <code>aborts_if</code> conditions are deleted at the same time, Prover will not report an error;</li><li>When adding <code>pragma aborts_if_is_partial</code>, no matter how many <code>aborts_if</code> conditions are kept, Prover will not report an error (of course, the conditions themselves must be correct).</li></ul><p>Some readers may be curious about the order of the three statements in the spec block:<br>Why the definition of balance can be written after <code>aborts_if !exists&lt;Balance&lt;CoinType&gt;&gt;(addr)</code>.<br>Because, if the latter holds true, <code>balance</code> does not actually exist.<br>Wouldn’t this order cause the Prover to fail?<br>Simply put: no, the statements in the spec block are declarative and the order doesn’t matter.</p><p>For a more detailed understanding, you can refer to the MSL documentation for more information.</p><h3 id="Specify-the-functional-nature-of-withdraw"><a href="#Specify-the-functional-nature-of-withdraw" class="headerlink" title="Specify the functional nature of withdraw"></a>Specify the functional nature of <code>withdraw</code></h3><p>Next, we define functional properties.<br>The two <code>ensures</code> statements in the following spec block give us what we expect from the <code>withdraw</code> functionality:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec withdraw &#123;    let balance &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    aborts_if !exists&lt;Balance&lt;CoinType&gt;&gt;(addr);    aborts_if balance &lt; amount;    let post balance_post &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    ensures balance_post &#x3D;&#x3D; balance - amount;    ensures result &#x3D;&#x3D; Coin&lt;CoinType&gt; &#123; value: amount &#125;;&#125;</code></pre><p>In this code, first by using <code>let post</code> binding, define <code>balance_post</code> as the balance of <code>addr</code> after the function is executed, it should be equal to <code>balance - amount</code>. Then, <code>result</code> is a special name that represents the return value, which should be the <code>amount</code> of tokens.</p><h2 id="Verify-the-deposit-function"><a href="#Verify-the-deposit-function" class="headerlink" title="Verify the deposit function"></a>Verify the <code>deposit</code> function</h2><p>The signature of the function deposit is as follows:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">fun deposit&lt;CoinType&gt;(addr: address, check: Coin&lt;CoinType&gt;) acquires Balance</code></pre><p>It deposits the token funds indicated by <code>check</code> into the address <code>addr</code>. Its canonical definition is as follows:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec deposit &#123;    let balance &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    let check_value &#x3D; check.value;    aborts_if !exists&lt;Balance&lt;CoinType&gt;&gt;(addr);    aborts_if balance + check_value &gt; MAX_U64;    let post balance_post &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    ensures balance_post &#x3D;&#x3D; balance + check_value;&#125;</code></pre><p>Here, <code>balance</code> is defined as the balance in <code>addr</code> before the function is executed, and <code>check_value</code> is defined as the amount of tokens to be deposited. It will abort in two cases:</p><ol><li>There is no resource of type <code>Balance&lt;CoinType&gt;</code> in <code>addr</code>;</li><li>Or the sum of <code>balance</code> and <code>check_value</code> is greater than the maximum value of type <code>u64</code>.</li></ol><p>The <code>ensures</code> statement is used to let the Prover make sure that in any case, the balance in <code>addr</code> can be updated correctly after the function is executed.</p><p>The syntax mentioned earlier will not be repeated here.<br>Astute readers may have noticed that it is worth noting that the expression <code>balance + check_value &gt; MAX_U64</code> is problematic in the Move program.<br>Because the addition on the left may cause an overflow exception.<br>If we want to write a similar check in the Move program, we should use an expression like <code>balance &gt; MAX_U64 - check_value</code> to avoid the overflow problem.</p><p>However, this expression is perfectly fine in the Move Specification Language (MSL).<br>Since the spec block uses the MSL language, its type system is different from that of Move.<br>In MSL, all integers are of type <code>num</code>, which is an integer in the mathematical sense. That is, it is signed and has no size limit.<br>All built-in integer types (<code>u8</code>, <code>u64</code>, etc.) are automatically converted to type <code>num</code> when referencing data in a Move program in MSL.<br>A more detailed description of the type system can be found in the MSL documentation.</p><h2 id="Verify-the-transfer-function"><a href="#Verify-the-transfer-function" class="headerlink" title="Verify the transfer function"></a>Verify the <code>transfer</code> function</h2><p>The signature of the function <code>transfer</code> is as follows:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">public fun transfer&lt;CoinType: drop&gt;(from: &amp;signer, to: address, amount: u64, _witness: CoinType) acquires Balance</code></pre><p>It is responsible for the transfer <code>from</code> the account from to the address <code>to</code>, and the transfer amount is <code>amount</code>.</p><p>Let’s ignore the abort condition for now and only consider its functional nature, and try to write its validation specification:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec transfer &#123;    let addr_from &#x3D; signer::address_of(from);    let balance_from &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr_from).coin.value;    let balance_to &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(to).coin.value;    let post balance_from_post &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr_from).coin.value;    let post balance_to_post &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(to).coin.value;    ensures balance_from_post &#x3D;&#x3D; balance_from - amount;    ensures balance_to_post &#x3D;&#x3D; balance_to + amount;&#125;</code></pre><p>Here <code>from</code> is of type <code>signer</code>, not a direct address.<br>Although in the program we have created a local variable called <code>addr_from</code>, we cannot directly reference it in the spec block.<br>At the same time, the expression of this address needs to be repeated several times, and it is very cumbersome to write repeatedly. We bind it to <code>addr_from</code> again.<br>Then use <code>let</code> and <code>let post</code> to define several variables, corresponding to the balances in the two addresses <code>addr_from</code> and <code>to</code> before and after the function is executed.<br>Finally, use the <code>ensures</code> statement to tell Prover that the balance in <code>from</code> should be subtracted by <code>amount</code>; the balance in <code>to</code> should be increased by <code>amount</code>.</p><p>At first glance, there seems to be no problem at all. But is it really so?<br>Let’s see if Prover thinks this is “the correct description of the behavior of this function”.<br>After typing <code>aptos move prove</code> you can see:</p><pre class="line-numbers language-none"><code class="language-none">error: post-condition does not hold   ┌─ .&#x2F;sources&#x2F;BasicCoin.move:58:9   │58 │         ensures balance_from_post &#x3D;&#x3D; balance_from - amount;   │         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^   │   &#x3D;     at .&#x2F;sources&#x2F;BasicCoin.move:45: transfer   &#x3D;     at .&#x2F;sources&#x2F;BasicCoin.move:51: transfer (spec)   &#x3D;     at .&#x2F;sources&#x2F;BasicCoin.move:53: transfer (spec)   &#x3D;     at .&#x2F;sources&#x2F;BasicCoin.move:54: transfer (spec)   &#x3D;     at .&#x2F;sources&#x2F;BasicCoin.move:45: transfer   &#x3D;         from &#x3D; signer&#123;0x0&#125;   &#x3D;         to &#x3D; 0x0   &#x3D;         amount &#x3D; 1   &#x3D;         _witness &#x3D; &lt;generic&gt;</code></pre><p>It is somewhat out of our expectations. Prover prompts that the postconditions are not satisfied, indicating that the behavior described in the previous spec block is not exactly the same as the <code>transfer</code> function.<br>Why is this so? Let’s look down again: the parameters that make the postconditions not satisfied are <code>from = signer&#123;0x0&#125;</code> and <code>to = 0x0</code>. We should know the reason: when the account transfers money to itself, both <code>to</code> and <code>from</code> point to the same address, so the balance does not change.</p><p>There are two solutions now:</p><p><strong>Plan A</strong> does not modify the function definition, but changes the specification.<br>In the spec block, consider whether the two accounts for the transfer and receiving are the same address.</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">let post eq_post &#x3D; balance_to &#x3D;&#x3D; balance_to_post;let post ne_post &#x3D; balance_from_post &#x3D;&#x3D; balance_from - amount                &amp;&amp; balance_to_post   &#x3D;&#x3D; balance_to   + amount;ensures (addr_from &#x3D;&#x3D; to &amp;&amp; eq_post) || (addr_from !&#x3D; to &amp;&amp; ne_post);</code></pre><p>Or use another slightly more intuitive if syntax:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">let post eq_post &#x3D; balance_to &#x3D;&#x3D; balance_to_post;let post ne_post &#x3D; balance_from_post &#x3D;&#x3D; balance_from - amount                &amp;&amp; balance_to_post   &#x3D;&#x3D; balance_to   + amount;ensures if (addr_from &#x3D;&#x3D; to) eq_post else ne_post;</code></pre><p>Note that <code>if (P) E1 else E2</code> is not the same as conditional execution in program logic – it’s actually a syntactic sugar equivalent to <code>ensures</code> both <code>P ==&gt; E1</code> and <code>!P ==&gt; E2</code>.<br>And <code>p ==&gt; q</code> is actually <code>!p || q</code>.</p><p>That is to say, the end of the second way of writing actually represents this logic:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">ensures (addr_from &#x3D;&#x3D; to  &#x3D;&#x3D;&#x3D;&gt;  eq_post) &amp;&amp; (addr_from !&#x3D; to  &#x3D;&#x3D;&#x3D;&gt; ne_post);</code></pre><p>that is:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">ensures (addr_from !&#x3D; to || eq_post) &amp;&amp; (addr_from &#x3D;&#x3D; to  || ne_post);</code></pre><p>Interested readers can verify it by themselves through the direct value table or by simplifying to normal form, the former <code>(addr_from == to &amp;&amp; eq_post) || (addr_from != to &amp;&amp; ne_post)</code> and the latter <code>(addr_from != to || eq_post) &amp;&amp; (addr_from == to  || ne_post)</code> are actually exactly equivalent expressions.</p><p><strong>Plan B</strong> does not modify the spec, but directly adds <code>assert!(addr_from != to, EEQUAL_ADDR);</code> in the function body, and adds the definition of the error code <code>EEQUAL_ADDR</code> in front, so that the self-transfer transaction cannot be completed.</p><p>Obviously, it is not meaningful to transfer money to yourself, so it is better to directly prohibit this kind of transaction.<br>So plan B is a better practice.<br>It directly guarantees that the two are definitely not the same address when successfully executed, and the code is more concise.</p><h3 id="Practice"><a href="#Practice" class="headerlink" title="Practice"></a>Practice</h3><p>Currently, we have only completed functional verification of the <code>transfer</code> function.<br>But it doesn’t say under what circumstances it will abort.<br>As an exercise, give it an appropriate <code>aborts_if</code> condition. The answer can be found in our next article.</p><h2 id="Verify-mint-function"><a href="#Verify-mint-function" class="headerlink" title="Verify mint function"></a>Verify mint function</h2><p>The signature of the function <code>mint</code> is as follows:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">public fun mint&lt;CoinType: drop&gt;(mint_addr: address, amount: u64, _witness: CoinType) acquires Balance</code></pre><p>It is responsible for minting the <code>amount</code> of tokens and depositing them in the address <code>mint_addr</code>.<br>More interesting is <code>_witness</code>, which is of type <code>CoinType</code>.<br>Because only the module that defines the <code>CoinType</code> can construct a value of this type, this guarantees the identity of the caller.</p><p>There is actually only one call to <code>deposit</code> in the <code>mint</code> function.<br>It is not difficult to imagine that there should be many similarities in the specifications to be satisfied by the two. Drawing a tiger according to a cat, it is not difficult to write:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec mint &#123;    let balance &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(mint_addr).coin.value;    aborts_if !exists&lt;Balance&lt;CoinType&gt;&gt;(mint_addr);    aborts_if balance + amount &gt; MAX_U64;    let post balance_post &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(mint_addr).coin.value;    ensures balance_post &#x3D;&#x3D; balance + amount;&#125;</code></pre><h2 id="Verify-the-publish-balance-function"><a href="#Verify-the-publish-balance-function" class="headerlink" title="Verify the publish_balance function"></a>Verify the <code>publish_balance</code> function</h2><p>The signature of the function <code>publish_balance</code> is as follows:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">public fun publish_balance&lt;CoinType&gt;(account: &amp;signer)</code></pre><p>It publishes an empty <code>Balance&lt;CoinType&gt;</code> resource under <code>account</code>.<br>So if the resource already exists it should exit abnormally, and end normally the balance should be zero:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec publish_balance &#123;    let addr &#x3D; signer::address_of(account);    aborts_if exists&lt;Balance&lt;CoinType&gt;&gt;(addr);    ensures exists&lt;Balance&lt;CoinType&gt;&gt;(addr);    let post balance_post &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    ensures balance_post &#x3D;&#x3D; 0;&#125;</code></pre><h2 id="Simplify-redundant-specifications-with-Schema"><a href="#Simplify-redundant-specifications-with-Schema" class="headerlink" title="Simplify redundant specifications with Schema"></a>Simplify redundant specifications with Schema</h2><p>Congratulations! So far, we have completed the verification of all the functions of BasicCoin step by step.<br>However, if you look closely at the code, many of the spec blocks look very similar, and the file structure would be clearer if they could be shortened a bit.</p><p>Schema is a means of building a specification by grouping properties.<br>Semantically, they are also syntactic sugar, and their use in a spec block is equivalent to expanding the conditions they contain into functions, structs, or modules.</p><h3 id="Eliminate-simple-repetitions"><a href="#Eliminate-simple-repetitions" class="headerlink" title="Eliminate simple repetitions"></a>Eliminate simple repetitions</h3><p>As a most obvious example, the spec blocks of <code>mint</code> and <code>deposit</code> are a little different except for the variable names (in terms, they are <a href="https://en.wikipedia.org/wiki/Lambda_calculus#%CE%B1-conversion">alpha convertible</a>), and the overall structure can be said to be exactly the same.<br>To simplify them, let’s create a Schema:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec schema DepositSchema&lt;CoinType&gt; &#123;    addr: address;    amount: u64;    let balance &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    aborts_if !exists&lt;Balance&lt;CoinType&gt;&gt;(addr);    aborts_if balance + amount &gt; MAX_U64;    let post balance_post &#x3D; global&lt;Balance&lt;CoinType&gt;&gt;(addr).coin.value;    ensures balance_post &#x3D;&#x3D; balance + amount;&#125;</code></pre><p>This Schema declares two typed variables, and some conditions about what those variables should satisfy.<br>When other places want to use this Schema, use <code>include DepositSchema &#123;addr: XX, amount: YY&#125;</code> to import it.<br>where <code>XX</code> and <code>YY</code> are expressions used to replace <code>addr</code> and <code>amount</code>.<br>If the expression is exactly the same as the corresponding variable name, you can just write the variable name, or simply omit it.</p><p>With the above Schema definition, we can now simplify the previous spec:</p><pre class="line-numbers language-rust" data-language="rust"><code class="language-rust">spec mint &#123;  include DepositSchema&lt;CoinType&gt; &#123;addr: mint_addr&#125;;&#125;&#x2F;&#x2F; ....spec deposit &#123;    include DepositSchema&lt;CoinType&gt; &#123;amount: check.value&#125;;&#125;</code></pre><h3 id="Practice-1"><a href="#Practice-1" class="headerlink" title="Practice"></a>Practice</h3><p>In addition to the above example, find another spec block (such as <code>publish_balance</code>), and split it into a Schema declaration and a spec block that uses the corresponding Schema.<br>As an exercise, the Schema you created might not be available in this code, so it doesn’t feel like there’s a benefit to it.<br>But if in the later development, there are other functions that call <code>publish_balance</code>, it will be more convenient.</p><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>In our article so far, we’ve shown you how the Move Prover works and it could potentially provide the ultimate safety guarantee for smart contracts. In the upcoming series, we will explore </p><ul><li>the tips and best practices; </li><li>a more complicated case study using the Move Prover; </li><li>the limitation of the current version of the Move Prover, and its future outlook;</li></ul><p>So stay tuned!</p><p>More and more developers are developing or deploying smart contracts on Move Ecosystem,  it is recommended to ship the Move contracts with a comprehensive security assessment using the Move Prover and other industry-leading audit methodology. If you need help evaluating Move smart contracts for vulnerabilities and certifying their behavior.  Please <a href="https://www.movebit.xyz/#contact">fill out our contact form</a>. We’d be happy to help.</p><h2 id="About-Us"><a href="#About-Us" class="headerlink" title="About Us"></a>About Us</h2><p><a href="https://www.movebit.xyz/">MoveBit</a> is a security audit company for the Move ecosystem. Our team is comprised of security leaders from academia and enterprise, with 10 years of cybersecurity experience, and has presented security research papers at top international security academic conferences such as NDSS and CS. </p><h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><ul><li><a href="https://github.com/move-language/move/tree/main/language/documentation/tutorial/">Move tutorial</a> by the Move team</li><li><a href="https://cookbook.starcoin.org/docs/move/move-prover/mvp-tutorial/">Starocin Move Prover tutorial</a> by Xudong Wang</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> move prover, m </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
